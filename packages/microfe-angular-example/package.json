{
    "name": "microfe-react-example",
    "version": "1.0.0",
    "private": true,
    "license": "ISC",
    "main": "index.js",
    "workspaces": [
        "projects/*"
    ],
    "resolutions": {
        "html-webpack-plugin": "4.5.1",
        "webpack": "5.15.0"
    },
    "scripts": {
        "init": "lerna init",
        "bootstrap": "lerna bootstrap",
        "dev": "lerna run --parallel dev",
        "dev:filter": "lerna run --parallel dev --scope mfe-shell --scope mfe-app-profile",
        "dev:shell": "lerna run --parallel dev --scope mfe-shell",
        "dev:profile": "lerna run  --parallel dev --scope mfe-app-profile",
        "build": "lerna run --parallel build",
        "serve": "lerna run --parallel serve"
    },
    "devDependencies": {
        "lerna": "^3.22.1"
    }
}